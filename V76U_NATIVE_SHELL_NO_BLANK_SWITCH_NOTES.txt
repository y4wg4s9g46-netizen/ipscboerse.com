V76U Native Shell No-Blank Switch

Ziel:
- Restflackern aus v76t reduzieren, das beim Wechsel zwischen Haupttabs sichtbar wurde.

Änderung:
- Der alte aktive iframe bleibt sichtbar, bis der neue iframe geladen, injiziert und gerendert ist.
- Neue Seiten werden als is-preparing unsichtbar vorbereitet.
- Erst nach load/ready wird per activateFrame gewechselt.
- Danach werden alte Frames als is-cached versteckt.
- Keine weißen/dunklen Curtains, kein Theme-Raten, kein harter Blank-Zustand zwischen Haupttabs.
- Browser/Desktop bleibt unverändert.

Wichtig:
- Weiterhin nur npx cap copy ios verwenden.
- Nicht npx cap sync ios verwenden.
